[project]
name = "cocoindex-ecommerce-taxonomy"
version = "0.1.0"
description = "Simple example for CocoIndex: extract taxonomy from e-commerce products and build knowledge graph."
requires-python = ">=3.11"
dependencies = ["cocoindex>=0.3.26,<1"]

[tool.setuptools]
packages = []
