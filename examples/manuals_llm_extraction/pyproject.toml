[project]
name = "manuals-llm-extraction"
version = "0.1.0"
description = "Simple example for cocoindex: extract structured information from a Markdown file using LLM."
requires-python = ">=3.11"
dependencies = ["cocoindex>=0.3.26,<1", "marker-pdf>=1.8.5"]

[tool.setuptools]
packages = []
