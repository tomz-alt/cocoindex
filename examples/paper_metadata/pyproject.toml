[project]
name = "paper-metadata"
version = "0.1.0"
description = "Build index for papers with both metadata and content embeddings"
requires-python = ">=3.11"
dependencies = [
    "cocoindex[embeddings]>=0.3.26,<1",
    "pypdf>=5.7.0",
    "marker-pdf>=1.8.5",
]

[tool.setuptools]
packages = []
